# ─── n8n Webhook Configuration ────────────────────────────────────────────────
# These URLs are required for the application to function
# Get these from your n8n instance after setting up the workflows

# Full multi-agent pipeline webhook (Agent 1 → 2 → 2b/3)
# Import full-pipeline.json into n8n
NEXT_PUBLIC_N8N_PIPELINE_WEBHOOK=https://your-n8n-instance.cloud/webhook/screening-pipeline

# Standalone scheduling webhook (Agent 3 only)
# Import agent3-interview-scheduler.json into n8n
NEXT_PUBLIC_N8N_SCHEDULING_WEBHOOK=https://your-n8n-instance.cloud/webhook/schedule-interview

# ─── Application Configuration ─────────────────────────────────────────────────

# Application name (displayed in navbar)
NEXT_PUBLIC_APP_NAME=Recruit AI

# Environment (development, staging, production)
NEXT_PUBLIC_APP_ENV=development

# ─── Optional: Google Calendar Integration ────────────────────────────────────
# Only needed if using Google Calendar for interview scheduling

# Your Google Calendar ID for scheduling
# NEXT_PUBLIC_GOOGLE_CALENDAR_ID=your-email@gmail.com

# ─── Optional: API Configuration ──────────────────────────────────────────────

# API timeout (in milliseconds)
# NEXT_PUBLIC_API_TIMEOUT=30000

# Enable debug logging
# NEXT_PUBLIC_DEBUG=true
